#WAP to count the number of students with the “A” grade in the following tuple. in python

# Tuple of student grades
grades = ("A", "B", "A", "C", "A", "B")

print(grades.count("A"))

# Count number of "A" grades
#count = grades.count("A")

# Display result
#print("Number of students with A grade:", count)
